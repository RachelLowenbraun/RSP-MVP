// FrameScheduler.cpp — all inline in the header for the M0 spike.
// This file exists so CMake sees a translation unit; keeps build config clean.
#include "FrameScheduler.h"
namespace rsp::timing {
// Currently no out-of-line definitions.
}
